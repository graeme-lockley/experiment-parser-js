-- given an import, declarations and expression
import file:test/lib/List as List;

lst = List.cons 1 (List.cons 2 List.empty);

lst
-- js
const List = require('./test/lib/List');

const lst = List.cons(1)(List.cons(2)(List.empty));

const _$EXPR = lst;

const _$ASSUMPTIONS = [].concat(
  List._$ASSUMPTIONS || []);

_$ASSUMPTIONS.push({
  source: 'stream',
  declarations: [

  ]
});


module.exports = {
  lst,
  _$EXPR,
  _$ASSUMPTIONS
};